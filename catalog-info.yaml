# DRAFT catalog-info.yaml for 'api-documentation' — generated by atlas from observed signals.
# Review every line. GUESS = inferred, verify it. Commented fields are REQUIRED
# but can't be inferred — uncomment and fill them with the team.
# How to fill each field + the full value lists: https://github.com/faurecia-aptoide/appning-atlas/blob/main/docs/guides/catalog-info.md
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: api-documentation
  # description:                 # one line — couldn't infer from GitHub
spec:
  type: documentation            # GUESS — one of: service | website | library | data-pipeline | infrastructure | experiment | documentation | tool | android-app — verify (see guide)
  lifecycle: active  # GUESS from activity — verify
  # owner:                      # TODO (REQUIRED): the owning TEAM (never a person)
  # escalationContact:          # TODO (REQUIRED): a team alias / rotation — never a person
  # dataSensitivity:            # TODO (REQUIRED): none | internal | sensitive — guide: docs/guides/data-sensitivity.md
  project: payments          # org extension — declared project membership
